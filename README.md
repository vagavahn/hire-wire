# hire-wire
This is a web based application that periodically pings an API called "Adzuna" to check for new job listings. Then parses the results, checks for duplicates, and then sends push request to a another API that sends the job listings via an email to the user. This application will also update a dashboard with the number of job listings that have been sent, completed, and not applied to.
